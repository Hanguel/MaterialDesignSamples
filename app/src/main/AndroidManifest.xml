<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.materiallist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialList">
        <activity
            android:name=".activity.appbarlayout.AppBarLayoutActivity"
            android:exported="false" />
        <activity
            android:name=".activity.viewpager.ViewPagerActivity"
            android:exported="false" />
        <activity
            android:name=".activity.viewholdertouchhelper.TouchHelperActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ComponentListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>